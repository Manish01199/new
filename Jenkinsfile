pipeline {
	agent any
	environment {
	   SKIP_PREFLIGHT_CHECK=true
	}

	stages {
	    stage('SCM Checkout') {

	    }
	    stage('Execute run shell') {
            steps{
                echo 'success'
                echo 'Done'
            }
        } 
   
}
}
