pipeline {
	agent any
	environment {
	   SKIP_PREFLIGHT_CHECK=true
	}

	stages {

	    stage('Execute run shell') {
            steps{
                sh '''
               
                '''
                echo 'Done'
            }
        } 
        //cp -r /var/lib/jenkins/workspace/node_modules/ /var/lib/jenkins/workspace/test/
        stage('Deploy') {
            steps {
                // Deploy your application using CloudFormation
                
            }
	}
}
}
